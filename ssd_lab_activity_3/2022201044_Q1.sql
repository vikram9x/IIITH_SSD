SELECT CONCAT(Fname, " ", Minit, " ", Lname) AS Full_Name, ssn, Dno, Dname FROM EMPLOYEE INNER JOIN DEPARTMENT ON EMPLOYEE.ssn = DEPARTMENT.mgr_ssn WHERE Dnumber in  (SELECT DISTINCT Dnum from WORKS_ON INNER JOIN PROJECT ON WORKS_ON.Pno = PROJECT.Pnumber WHERE WORKS_ON.Hours < 40);
